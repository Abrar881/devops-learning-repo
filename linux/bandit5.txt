## Bandit Level 5 → Level 6

**Challenge:** Find a file with these properties:
- Human-readable
- 1033 bytes in size
- Not executable

**Solution:**
```bash
find . -type f -size 1033c ! -executable -exec file {} \; | grep text
cat ./maybehere07/.file2

**Explanation**:

find . -type f -size 1033c searches for files exactly 1033 bytes

**Password:** [HWasnPhtq9AVKe0dmk45nxy20cvUa6EG]

**What I learned:** Using the command find and the properties needed I am able to identify the required file
